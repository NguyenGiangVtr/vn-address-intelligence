# `scripts/scratch` — thử nghiệm cục bộ

Các script từng nằm ở `scratch/` (đã dời vào đây để gọn gốc repo).

- Không dùng trong pipeline production.
- Mỗi file có bootstrap `_repo_bootstrap` — chạy từ repo root:  
  `python scripts/scratch/check_db.py`
